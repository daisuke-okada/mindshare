<h1>Actions detail</h1>

<ul>
	<li>
		<p>Status</p>
		<%= @post.status %>
	</li>
	<li>
		<p>Action</p>
		<%= @post.content %>
	</li>
	<li>
		<p>User</p>
		<%= @post.user.name%>
	</li>

	<% if @post.user.id == current_user.id %>
	
	<%= link_to "edit", edit_post_path(@post) %>
	
	<% end %>
</ul>
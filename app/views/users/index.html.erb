
<h1>User index</h1>

<ul>
	<% @users_pos.each do |user_pos| %>
	<li>
		<%= user_pos.name %>
		<%= link_to "detail", "/users/#{current_user.id}" %>
	</li>
	<% end %>
</ul>

<ul>
	<% @users_neg.each do |user_neg| %>
	<li>
		<%= user_neg.name %>
		<%= link_to "detail", "/users/#{current_user.id}" %>
	</li>
	<% end %>
</ul>